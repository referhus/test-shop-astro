# test-shop-astro

## Project Setup

```sh
npm install
```

### Compile and Hot-Reload for Development

```sh
npm run dev
```

### Type-Check, Compile and Minify for Production

```sh
npm run build
```

## Project Info
1. Отобразить вид сеткой/списком
- реализовать компонент переключения отображения карточек сеткой/списком (реализован данный вариант)
- отобразить оба варианта друг за другом

2. Повторное добавление товара в корзину (при смене цвета):
- можно добавлять один и тот же товар с разными ценой/цветом
- нельзя добавить товар с другим цветом/ценой, при попытке - предыдущий 
добавленный товар с данным id заменится на выбранный (реализован данный вариант)

3. Поведение кнопки после добавления товара в корзину:
- скрыть отображение кнопки вообще
- заменить текст и логику на удаление товара из корзины (реализован данный вариант)

4. Отображение ссылки на корзину в хедере в ssr:
- отображается изначально, счетчик отображается в onMounted (реализован данный вариант)
- не отображается изначально, так как является client:only компонентом

5. Отображение кнопки добавления в корзину в ssr:
- отображается изначально с лоадером, правильный текст загружается в onMounted (реализован данный вариант)
- не отображается изначально, так как является client:only компонентом

6. Add to Card - кнопка переименована, возможно в макете опечатка, Cart - корзина